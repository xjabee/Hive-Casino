const addevenmorepoints = require("./addevenmorepoints");
const addpoints = require("./addpoints");
const daily = require("./daily");


module.exports = [
    addpoints,
    addevenmorepoints,
    daily
]